export type Props = {
    children?: React.ReactNode
    src?: string
    
}
export type Image = {
    src: string,
    description: string,
    title: string
    backImg: string
    banner: string
    isMain: boolean
}